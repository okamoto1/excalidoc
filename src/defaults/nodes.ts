export enum ExcalidocDefaultNodeTypes {
  Service = "service",
  UseCase = "usecase",
  Factory = "factory",
  DomainModel = "domain-model",
  DomainEvents = "domain-events",
  DatabaseModel = "database-model",
}
